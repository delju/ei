{% extends 'layouts/user.html.twig' %}
{% block title %} CopAnimaux - Notre Mission {% endblock title %}

{% block page %}


    <section class="m-page">
        <div id="myCarousel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img class="d-block w-100" src="{{ asset('user/images/home/Dogandcat.jpg') }}" alt="First slide">
                </div>
                <div class="carousel-item">
                    <img class="d-block w-100" src="{{ asset('user/images/home/dogs.jpg') }}" alt="Second slide">
                </div>
                <div class="carousel-item">
                    <img class="d-block w-100" src="{{ asset('user/images/home/cats.jpg') }}" alt="Third slide">
                </div>
            </div>
        </div>
        <div class="container">
            <h1 class="title-home">Bienvenue</h1>
            <div class="text-slide">
                <p class="">
                    CopAnimaux est une ASBL fondée en 1989 et qui depuis ne cesse de faire évoluer le bien-être animal.
                </p>
                <p>
                    Notre mission est de créer un endroit pour les animaux perdus, saisis ou abandonnés, et de leurs
                    donner un nouvel avenir. Mais nous aidons également les propriétaires d'animaux en leur fournissant
                    des conseils.
                </p>
            </div>
        </div>
    </section>
    <section class="last-chance-section">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="text-center">
                        <h4>Les adoptions de la dernière chance</h4>
                    </div>
                    <p>
                        Ces animaux n'ont pas toujours eu la vie facile, et aujourd'hui, par vieillesse ou maladie, ils
                        ont besoin d'un suivi spécifique. À vous de leur donner une dernière chance de vivre dans un
                        foyer paisible.
                    </p>
                </div>
            </div>
            <div class="col-lg-12 mt-4">
                <div class="row justify-content-xxl-center">
                    {# Boucle sur les derniers sortis #}

                    {% for lastChances in lastChance %}
                        <div class="col-sm-12 col-lg-3">
                            <!-- product card -->
                            <div class="product-item bg-light">
                                <div class="card">
                                    <div class="thumb-content">
                                        <a href="{{ url('single', {slug : lastChances.slug} ) }}">
                                            <img class="card-img-top img-fluid"
                                                 src="{{ asset('uploads/'~lastChances.gallery.photos[0].url) }}"
                                                 alt="Card image cap">
                                        </a>
                                    </div>
                                    <div class="card-body text-center">
                                        <h4 class="card-title ">
                                            <a class="lodger-card"
                                               href="{{ url('single', {slug: lastChances.slug}) }}">{{ lastChances.name | capitalize }}
                                                | {{ lastChances.birthdate | age }} an(s)</a>
                                        </h4>
                                        <p>
                                            {{ lastChances.race }}
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </section>
{% endblock page %}