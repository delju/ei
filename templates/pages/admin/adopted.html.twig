{% extends 'layouts/admin.html.twig' %}

{% block title %} Admin - Adoptés{% endblock title %}

{% block page%}

    {% include 'elements/lodgers-card.html.twig' %}

    <!-- pagination -->
    <div class="self-pagination row justify-content-center ">
        {{ knp_pagination_render(lodgers) }}
    </div>

{% endblock %}