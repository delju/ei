{% extends 'layouts/admin.html.twig' %}

{% block title %} Admin - Retrouvés{% endblock title %}

{% block page%}
{#Titre#}
    <h4>Retrouvés</h4>
{#Affichage du message lors de l'ajout d'un pensionnaire dans la liste#}
    {% for message in app.flashes('addRecovered') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}

{#    Affichage des pensionnaires#}
    {% include 'elements/lodgers-card.html.twig' %}


    <!-- pagination -->
    <div class="self-pagination row justify-content-center ">
        {{ knp_pagination_render(lodgers) }}
    </div>

{% endblock %}